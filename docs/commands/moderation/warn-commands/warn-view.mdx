---
sidebar_position: 3
---

# /warn view 

View all warnings a user has received, listed in order with their associated reasons and case numbers.

<hr className="md-divider-gradient" />

### 💬 Usage
```bash
/warn view <user>
```
- `<user>` – The member whose warning history you want to review.

:::success
Only users with moderation permissions can use this command.
:::

<hr className="md-divider-gradient" />

### 📌 Example
```bash
/warn view @DisruptiveUser
```
This will:
- Display a list of all warnings issued to @DisruptiveUser
- Its number (e.g., Warning #1, #2, etc.)
- Reason
- Moderator who issued it
- Date issued

<hr className="md-divider-gradient" />

### 🗃️ Notes
> - Warnings are ordered numerically, with the most recent usually at the bottom.
> - You can use these warning numbers with /warn remove to delete specific entries.
> - If the user has no warnings, Pandoryx will let you know.

<hr className="md-divider-gradient" />

### 🔍 Related Commands
`/warn add` – Issue a new warning

`/warn remove` – Remove a specific warning by number