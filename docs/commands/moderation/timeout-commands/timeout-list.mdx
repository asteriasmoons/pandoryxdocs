---
sidebar_position: 2
---

# /timeout list

Display a list of all users currently timed out in your server. This command is part of the Moderation module.

<hr className="md-divider-gradient" />

### 💬 Usage
```bash
/timeout list
```
:::success
No additional input required. This will pull up a live list of all active timeouts handled by Pandoryx.
:::

<hr className="md-divider-gradient" />

### 📌 Example
```bash
/timeout list
```
This will:
- Show all users who are currently timed out
- Display basic info for each case, including:
- User ID
- Remaining time
- Reason (if available)
- Case number

<hr className="md-divider-gradient" />

### 🗃️ Notes
> - This list only includes active timeouts. Once a timeout expires or is removed, it will no longer appear here.
> - You can use the case number with /timeout view to see full details, or /timeout remove to end it early.
> - If there are no current timeouts, the bot will let you know.

<hr className="md-divider-gradient" />

### 🔍 Related Commands
`/timeout add` – Add a timeout to a user

`/timeout remove` – Remove a user’s timeout early

`/timeout view` – View the details of a timeout case